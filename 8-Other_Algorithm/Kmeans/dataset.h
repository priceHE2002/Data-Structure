/**
 * dataset.h — 数据集统一接口
 *
 * 每个数据集需提供:
 *   - DIM   : 特征维度
 *   - SIZE  : 样本数量
 *   - NUM_CLASSES : 真实类别数
 *   - load(double features[SIZE][DIM], int labels[SIZE]) : 加载数据
 */

#ifndef DATASET_H
#define DATASET_H

#endif
